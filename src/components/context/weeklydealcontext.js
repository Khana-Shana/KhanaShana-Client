/* context for accessing weekly deals availed from landing in the cart */

import React, {createContext} from 'react';

const WeeklyDealContext = createContext();

export default WeeklyDealContext;