/* context to make discount availed from wheel global for access by the cart */

import React, { createContext } from "react";

const DiscountContext = createContext();

export default DiscountContext;
